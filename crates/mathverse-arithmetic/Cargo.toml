[package]
name = "mathverse-arithmetic"
version = "0.1.1"
edition = "2021"
rust-version.workspace = true
description = "MathVerse arithmetic: percentages, powers, roots, modulus, re-exports of core scalar ops"
license = "MIT OR Apache-2.0"
repository = "https://github.com/Rohithdgrr/Rust-math"
keywords = ["math", "arithmetic", "percentage", "powers", "roots"]
categories = ["science", "mathematics"]

[dependencies]
mathverse-core = { path = "../mathverse-core", version = "0.1.1" }

[lints]
workspace = true
