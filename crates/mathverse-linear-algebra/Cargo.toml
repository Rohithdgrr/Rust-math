[package]
name = "mathverse-linear-algebra"
version = "0.1.0"
edition = "2021"
rust-version.workspace = true
description = "MathVerse linear algebra: unified entry point for matrices, vectors, decompositions"
license = "MIT OR Apache-2.0"
repository = "https://github.com/Rohithdgrr/Rust-math"
keywords = ["math", "linear-algebra", "matrices", "vectors", "decomposition"]
categories = ["science", "mathematics"]

[dependencies]
mathverse-core = { path = "../mathverse-core", version = "0.1.0" }
mathverse-vector = { path = "../mathverse-vector", version = "0.1.0" }
mathverse-matrix = { path = "../mathverse-matrix", version = "0.1.0" }

[lints]
workspace = true
