[package]
name = "mathverse-graphics"
version = "0.1.0"
edition = "2021"
description = "MathVerse graphics math: affine transforms, quaternions, projection"
license = "MIT OR Apache-2.0"

[dependencies]
mathverse-core = { path = "../mathverse-core" }
mathverse-matrix = { path = "../mathverse-matrix" }
mathverse-vector = { path = "../mathverse-vector" }
