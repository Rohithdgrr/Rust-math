[package]
name = "mathverse-lazy"
version = "0.1.0"
edition = "2021"
rust-version.workspace = true
description = "MathVerse lazy evaluation: expression templates, deferred computation, fused operations"
license = "MIT OR Apache-2.0"
repository = "https://github.com/Rohithdgrr/Rust-math"
keywords = ["math", "lazy", "expression-templates", "optimization", "fusion"]
categories = ["science", "mathematics"]

[features]
default = ["std"]
std = []

[dependencies]
mathverse-core = { path = "../mathverse-core", version = "0.1.0" }

[lints]
workspace = true
