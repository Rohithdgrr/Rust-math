[package]
name = "mathverse-trigonometry"
version = "0.1.0"
edition = "2021"
description = "MathVerse trigonometry: circular, hyperbolic, inverse functions, angle conversions"
license = "MIT OR Apache-2.0"

[dependencies]
mathverse-core = { path = "../mathverse-core" }
