[package]
name = "mathverse-number-theory"
version = "0.1.0"
edition = "2021"
description = "MathVerse number theory: primes, factorization, modular arithmetic, totient"
license = "MIT OR Apache-2.0"

[dependencies]
mathverse-core = { path = "../mathverse-core" }
