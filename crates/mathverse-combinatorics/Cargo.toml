[package]
name = "mathverse-combinatorics"
version = "0.1.0"
edition = "2021"
description = "MathVerse combinatorics: permutations, combinations, Catalan, Stirling numbers"
license = "MIT OR Apache-2.0"

[dependencies]
mathverse-core = { path = "../mathverse-core" }
